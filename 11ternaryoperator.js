{//soal 1
    const bil = 12;
    const result = bil >=10 ? `angka lebih besar dari 10` : `angka lebih kecil dari 10`;
    console.log(result);
}


{//soal 2
    const bil = -12;
    const result = bil %2 !== 0 ? 'bilangan ganjil' : 'bilangan genap'
    console.log(result);
}


{//soal 3
    const hasAcces = true;
    const result = hasAcces ? `Anda memiliki akses` : `Anda tidak memiliki akses`;
    console.log(result);
}


{//soal 4
    const nilai = 69;
    const result = nilai>=90 && nilai<=100? 'A' : nilai >=80 ? 'B' : nilai >= 70 ? 'C' : 'D';
    console.log(result);
}


{//soal 5
    const isAuthorized = false;
    const result = isAuthorized ? `Anda memiliki izin` : `Anda tidak memiliki izin`;
    console.log(result);
}